
export type Student = {
    id: string;
    name: string;
    profil: string; 
    seances: boolean[]; // Array of boolean values for completed sessions
    sorties: boolean[];
    tel: string;
    email : string ;
  }